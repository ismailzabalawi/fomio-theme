import Component from "@glimmer/component";

// Preferences render through `above-user-preferences`. Leave this connector
// inert so profile pages do not show the settings surface in the wrong outlet.

export default class FomioPreferencesTab extends Component {
  <template></template>
}
