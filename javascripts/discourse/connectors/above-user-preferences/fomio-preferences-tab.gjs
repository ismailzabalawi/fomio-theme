import Component from "@glimmer/component";

// Preferences stay on native Discourse routes. This outlet is intentionally
// empty so future work can add route-local enhancements without replacing the
// route body or save flows.

export default class FomioPreferencesTab extends Component {
  <template></template>
}
