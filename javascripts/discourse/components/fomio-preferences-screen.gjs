import Component from "@glimmer/component";

// Deprecated scratch component.
// Preferences now remain native Discourse routes styled through
// `body.user-preferences-page` and verified plugin outlets. This file is left
// inert on purpose so future work does not treat it as a supported route
// implementation.

export default class FomioPreferencesScreen extends Component {
  <template></template>
}
